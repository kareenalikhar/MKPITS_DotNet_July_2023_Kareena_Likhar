create table products(prodid int primary key,prodname varchar(50),price int ,quantity int);

select * from products

create table customer(custid int primary key,custname varchar(50),custaddress varchar(50),mobileno bigint,emailid varchar(50));

select * from customer

